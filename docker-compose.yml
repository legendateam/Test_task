version: "3.7"

services:
  app:
    build:
      context: .
    restart: on-failure
    env_file:
      - .env
    volumes:
      - ./backend:/app
      - /app/node_modules
    command: >
      /wait-for-it.sh db:5432 -- npm run start:dev

  db:
    image: postgres:14.3-alpine
    restart: on-failure
    env_file:
      - .env.-db
    ports:
      - "5433:5432"
    volumes:
      - ./db:/var/lib/postgresql/data
